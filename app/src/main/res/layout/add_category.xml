<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:background="@drawable/login_background"
    android:padding="16dp">

    <ImageButton
        android:layout_marginTop="16dp"
        android:id="@+id/add_category_image"
        android:layout_width="144dp"
        android:layout_height="144dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/baseline_add_a_photo_24"
        android:background="@color/white"
        android:scaleType="centerInside"/>

    <EditText
        android:id="@+id/add_category_name"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:textColor="@color/white"
        android:textColorHint="@color/white"
        android:layout_marginTop="16dp"
        android:ems="10"
        android:hint="category name"
        android:inputType="text"
        android:textAlignment="center"
        app:layout_constraintEnd_toEndOf="@+id/add_category_image"
        app:layout_constraintStart_toStartOf="@+id/add_category_image"
        app:layout_constraintTop_toBottomOf="@+id/add_category_image" />

    <Button
        android:layout_marginTop="32dp"
        android:id="@+id/confirm_category_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Category"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/add_category_name" />

    <Button
        android:id="@+id/delete_category_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Delete Category"
        app:layout_constraintTop_toBottomOf="@id/confirm_category_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>